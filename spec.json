{
  "name":"testbuildpack",
  "environment":[{
    "key": "SOMEENV",
    "default": "SOMEDEFVAL"
  }],
  "detect": {
    "exists": [{
      "path": "check.test"
    }],
    "scripts": [{
      "command": "echo test2"
    }]
  },
  "build":[{
    "remote": [{
      "url": "https://sh.rustup.rs",
      "path": "rustup.rs"
    }],
    "scripts": [{
      "command": "touch samplefile.test"
    }]
  }]
}
