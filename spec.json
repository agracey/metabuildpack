{
  "name":"testbuildpack",
  "environment":[{
    "key": "SOMEENV",
    "default": "SOMEDEFVAL"
  }],
  "detect": {
    "exists": [{
      "path": "check.test"
    }],
    "scripts": [{
      "command": "echo test2"
    }]
  },
  "build":[{
    "remote": [{
      "url": "https://sh.rustup.rs",
      "path": "rustup.sh"
    }],
    "local": [{
      "from":"wholedir",
      "to": "test2.tmpdir"
    }],
    "scripts": [{
      "command": "touch samplefile.test"
    }]
  }]
}
